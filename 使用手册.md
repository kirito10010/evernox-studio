# EverNox（永夜照相馆）使用手册

> 一个自托管的个人数字生活管理平台：图床相册、网站导航、记事本、记账、绩效与工资、话题社区、火影忍者OL 图鉴/测验/组织积分。

---

## 目录

1. [快速开始](#1-快速开始)
2. [账号体系](#2-账号体系)
3. [图床管理](#3-图床管理)
4. [网站分享](#4-网站分享)
5. [火影忍者ol](#5-火影忍者ol)
6. [个人工作台](#6-个人工作台)
7. [话题集中营](#7-话题集中营)
8. [管理员后台](#8-管理员后台)
9. [组织积分（重点）](#9-组织积分重点)
10. [组织积分 Excel 导入](#10-组织积分-excel-导入)
11. [常见问题](#11-常见问题)

---

## 1. 快速开始

### 环境要求
- JDK 21
- Maven 3.8+
- Node.js 18+（含 npm）
- MySQL 8.0

### 启动
- Windows 下双击根目录 `start.bat`，会自动清理端口并启动前后端。
- 前端地址：`http://localhost:5211`
- 后端地址：`http://localhost:11002/api`

### 首次使用
1. 启动后会自动建库建表（`schema.sql`）。
2. 启动时还会根据外置配置 `evernox-backend/config/application.yml` 里的 `evernox.admin.*` **自动注册管理员账号**：
   - 账号不存在 → 自动创建（角色 admin）。
   - 账号已存在 → 自动同步角色/状态/邮箱，并**按配置重置密码哈希**。
   - 管理员登录账号即该配置里的 `username` / `password`。
3. 也可打开 `http://localhost:5211`，点「立即注册」注册普通账号。

> 需要临时提权为管理员时，可在数据库执行：
> ```sql
> UPDATE `user` SET `role` = 'admin' WHERE `username` = '你的账号';
> ```
> 然后重新登录（角色写在 JWT 里，旧 token 需重新登录才生效）。

---

## 2. 账号体系

### 注册
- 用户名：3–50 字符，只能字母、数字、下划线。
- 邮箱：需唯一。
- 密码：8–100 字符，**必须同时包含大小写字母和数字**。

### 登录
- 用户名 + 密码。
- 连续输错 5 次会锁定 15 分钟。

### 找回密码
1. 登录页点「忘记密码？」进入找回页。
2. 输入**注册时填写的邮箱** → 点「发送验证码」。
3. 到邮箱收取 6 位验证码（10 分钟有效，163 发信）。
4. 输入验证码 + 新密码 → 重置成功。

### 角色
| 角色 | 权限 |
|---|---|
| member（普通成员） | 使用全部前台功能 |
| super_member（超级会员） | 前台 + 部分特权（可管理组织积分） |
| admin（管理员） | 全部 + 管理员后台 |

### 积分与会员
- **每日签到**：在「个人信息」页点「每日签到」，每次 +10 积分，一天一次。
- **个人信息**：右上角头像 → 个人信息，可查看积分、修改密码。
- **超级会员**：管理员在「积分与会员管理」里给用户设置超级会员时长（一周/一月/三月/一年/自定义天数）；到期后立即失效，重新登录或使用时自动校验。
- **积分充值**：管理员在「积分与会员管理」里搜索用户后充值积分（固定档位或自定义金额）。

---

## 3. 图床管理

- **上传图片**：支持多张上传，自动生成缩略图，图片加密后落盘存储。
- **相册**：创建相册、批量管理、封面裁剪。
- **可见性**：图片/相册可设为「公开」或「私密」。
- **入口**：首页 / 公开图床 / 公开相册 / 我的图床 / 我的相册 / 图片详情。

---

## 4. 网站分享

- 添加「友链」（网址 + 标题 + 标签）。
- 提交后进入**管理员审批**，审批通过后公开可见。
- 入口：网站导航（公开）/ 我的分享。

---

## 5. 火影忍者ol

| 功能 | 说明 |
|---|---|
| 公告 | 查看官方公告 |
| 忍者图鉴 | 查看忍者 + 技能 |
| 忍者测验 | 答题测验 |
| **组织积分** | 只读看板：按组织查看每周积分、排序、柱状图 |

### 组织积分看板（普通用户）
1. 顶部选「组织」。
2. 默认显示最新一周（有本周显示本周，否则显示上周）。
3. 表格可点击列头排序，默认按「总积分」降序。
4. 上方柱状图展示各成员总积分。

---

## 6. 个人工作台

| 功能 | 说明 |
|---|---|
| 记事本 | 富文本 + 图片，可提交公开（需管理员审批） |
| 待办 | 内容、优先级、截止日期、完成状态 |
| 记账 | 分类 + 记录 + 支出图表 |
| 记录绩效 | 项目 / 绩效记录 / 加班 / 迟到 + 图表 |
| 记录工资 | 工资配置 + 记录 + 工资曲线图 |

---

## 7. 话题集中营

- **圈子**：创建圈子、加入圈子。
- **帖子**：发帖、回复。
- **互动**：评论、点赞、收藏。
- 入口：广场 / 圈子 / 我的。

---

## 8. 管理员后台

| 菜单 | 说明 |
|---|---|
| 用户管理 | 用户列表、搜索、创建、编辑、启用/禁用、删除、批量删除 |
| 相册图床 | 管理所有用户的图片/相册 |
| 网站审批 | 审批友链（通过/驳回） |
| 笔记审批 | 审批记事本公开申请 |
| 公告 | 发布/管理公告 |
| 话题集中营管理 | 管理圈子/帖子/评论 |
| 忍者测验管理 | 题目增删改查、审批、Excel 批量导入 |
| 组织积分 | 见下一节 |

---

## 9. 组织积分（重点）

面向火影忍者OL 游戏组织（公会）的周积分统计与功勋礼包发放。

> 入口：侧边栏「超级会员 → 组织积分」。管理员/超级会员可直接使用；普通成员只能看到「说明书」，需使用积分升级为超级会员后才可使用（升级功能开发中）。

### 整体流程
```
① 添加组织 → ② 添加成员（选组织） → ③ 配置积分换算比/礼包
→ ④ 一键生成某周记录 → ⑤ 导入 Excel 填活动数据
→ ⑥ 计算积分 → ⑦ 逐个设置礼包 → ⑧ 下一周自动流转
```

### 9.1 组织管理
- 「添加组织」：创建组织时自动初始化**默认换算比**和**六个默认礼包**（本服/跨服 × 传说/英雄/精英）。
- 「管理组织」：编辑、删除组织（组织下还有成员时禁止删除）。

### 9.2 成员管理
- 添加成员：选择所属组织 + 玩家名 + 职务。
- 编辑成员：改名 / 改职务 / 改组织。
- **状态**：在组织 / 已离开。点「离开」= 假删除（保留历史），可「恢复」重新加入。
- 已离开的成员**不参与一键生成**。
- **Excel 批量导入**：先选「导入到组织」→「选择 Excel」→「导入成员」。
  - 玩家名列表头任选其一：`玩家名称 / 玩家名 / 成员 / 名字 / 玩家`。
  - 可选「职务」列（表头 `职务` 或 `职位`），没有则职务留空。
  - 导入的成员默认状态为「在组织」。
  - 重名、名字/职务超 50 字符 → 跳过并显示在「未导入」名单。

### 9.3 积分换算比（每个组织一套）
配置每个活动「每 1 单位 = Y 积分」，每项可单独开关：

| 活动 | 默认 |
|---|---|
| 忍战次数 | 1 次 = 20 积分 |
| 总战力 | 1 战力 = 0.00005 积分 |
| 战力增幅 | 1 战力 = 0 积分 |
| 铜币贡献 | 1 捐献 = 0.02 积分 |
| 通灵兽献祭 | 1 献祭 = 0.01 积分 |
| 叛忍 | 1 次 = 3 积分 |
| 叛忍车头「是」 | +50 积分 |

- 每项有两个独立开关：**启用**（是否参与积分计算）和**显示列**（表格里是否显示该列）。
- 「显示列」关闭只影响「周记录」表和「组织积分」表的列显示，不影响 Excel 导入与积分计算。

### 9.4 奖励礼包（每个组织一套）
默认六个礼包：

| 礼包 | 扣除比例 |
|---|---|
| 本服-传说功勋礼包 | 65% |
| 本服-英雄功勋礼包 | 55% |
| 本服-精英功勋礼包 | 45% |
| 跨服-传说功勋礼包 | 75% |
| 跨服-英雄功勋礼包 | 65% |
| 跨服-精英功勋礼包 | 55% |

- 可增删改，扣除比例 = 扣掉总积分的百分比。

### 9.5 周记录
- **一键生成**：先选组织，可选日期（默认本周周日）；为「在组织」的成员各生成一条记录，自动填入玩家名、职务、上周剩余积分。
- **上周剩余积分自动流转**：
  - 上一周**已领礼包**的成员：继承「扣除后总积分」。
  - 上一周**未领礼包**的成员：继承「总积分」（未扣除）并额外加减「未领礼包积分调整」值。
  - 无上一周记录时按 0 处理。
- **未领礼包积分调整**：在「积分换算比」Tab 里设置（可正可负），仅对未领礼包的成员生效。
- **导入数据**：上传 Excel（见下一节）。
- **计算积分**：按换算比计算「本周积分 / 总积分」。
- **设置礼包**：每条记录下拉选礼包，自动算「扣除后总积分」。
- **删除本周**：删除整周批次以便重新生成。
- **历史周**：顶部周下拉可回看任意历史周。

### 9.6 组织可见性与加入审批
- 管理员创建的每个组织会记录「创建者」，看板（火影忍者OL → 组织积分）**对管理员全部可见**。
- 普通成员查看某组织数据前，需先**申请加入**该组织（单选一个），由**创建该组织的管理员**审批通过后才能查看。
- 有用户申请时，会给创建者管理员的邮箱**发送提醒邮件**。
- 管理员在「加入审批」Tab 查看本人组织下的待审批申请，可「通过 / 拒绝」。
- 普通成员可**直接退出**已加入的组织（无需审批）；被拒绝后可重新申请。

### 计算公式
```
本周积分 = Σ(开启项：数量 × 积分) + (车头开启且是车头 ? 50 : 0)
总积分   = 上周剩余积分 + 本周积分
扣除后总积分 = 总积分 × (1 − 礼包扣除比例)
```
- 积分保留 5 位小数。
- 总战力参与计算（默认 0.00005/战力，可关闭或调整）。

---

## 10. 组织积分 Excel 导入

### 导入步骤
1. 先在「周记录」里**一键生成**某周记录。
2. 准备 `.xlsx` 文件：**第一行必须是表头**，第二行开始是数据。
3. 点「选择 Excel」→「导入数据」。
4. 导入后弹窗展示：成功导入、未导入、仍为空数据三份名单（需手动关闭）。

### 表头对照表

| 要导入的字段 | 可用的表头写法（任选其一） |
|---|---|
| **玩家名称（必填，匹配键）** | 玩家名称 / 玩家名 / 成员 / 名字 / 玩家 |
| 忍战次数 | 忍战次数 / 忍战活动次数 / 忍战 |
| 总战力 | 总战力 / 战力 |
| 战力增幅 | 战力增幅 / 战力增长 / 战力增加 |
| 铜币贡献 | 铜币贡献 / 铜币捐献 / 铜币 |
| 通灵兽献祭 | 通灵兽献祭 / 通灵兽 / 通灵 |
| 叛忍次数 | 叛忍次数 / 叛忍 |
| 叛忍车头 | 叛忍车头 / 车头 |

### 最小可用示例（两列）

```
玩家名称	忍战次数
爆走の八神	4
江夏平左	4
咕咕大王	3
```

### 注意事项
1. 文件必须是 `.xlsx`（不是 `.xls` 或 `.csv`）。
2. 第一行就是表头，不要加标题行。
3. 玩家名要和系统里**完全一致**（含日文假名、符号、空格）。
4. 职务、积分、礼包**不需要导入**（来自成员配置或计算得出）。
5. 未匹配的名字会在弹窗里列出。

---

## 11. 常见问题

### Q1：修改后端代码后接口报「字段不存在/列不存在」？
多半是 `mvn spring-boot:run` 没重编译。先执行 `mvn clean compile` 再重启后端。

### Q2：改了表结构后报错？
组织积分相关表结构变更后，需要**删库重建**（或删掉 `org_*` 表）再重启，让 `schema.sql` 重新建表。

### Q3：下周的「上周剩余」为什么是 0？
因为上周还有成员**没设置礼包**（扣除后积分为空）。补设礼包后重新生成即可。

### Q4：成员改名了积分会丢吗？
不会。积分按成员 ID 关联，历史各周保留名字快照。改名请用「编辑」，不要删除重建。

### Q5：敏感配置在哪？
`evernox-backend/config/application.yml`（数据库口令、图片编解码密钥、SMTP 账号、管理员初始账号）。该文件**不要提交到代码仓库**。
